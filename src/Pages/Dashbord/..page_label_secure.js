/**
 * 1. do not show the link to thos(not admin user) user who should not see the link
 * 2.even if they gets the link,do not allow them to visit the link (use AdminRoute Same work the Private Route)
 * 3. do not allow user to access the api check admin in the server as well 
 * (verifyAdmin)
 */